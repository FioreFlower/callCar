{
  "name": "call-car",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "npx wrangler pages dev public",
    "deploy": "npx wrangler pages deploy public --project-name callcar --branch main"
  },
  "engines": {
    "node": ">=20"
  }
}
